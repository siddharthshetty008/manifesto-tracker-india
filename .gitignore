# Node / web scaffolding (if added later)
node_modules/
.next/
dist/
build/
.vercel/

# Env / secrets
.env
.env.local
.env.*.local

# Python (if scripts added)
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log

# Large raw sources — track only processed/structured data
data/raw/**/*.pdf
data/raw/parliament/

# Per-run artifacts (committed selectively, not en masse)
scripts/_runs/

# Draft selection log — machine-generated, awaits human review before
# promotion to docs/selection-log.md (which IS committed)
docs/selection-log.draft.md
